import { Box, Button } from '@mui/material'
import React from 'react'

const externalLogin = ({socket}) => {
  return (
    <Box >
    <Button>Chat</Button>
    </Box>
  )
}

export default externalLogin